package com.devsu.cuenta.domain.repository;

import com.devsu.cuenta.domain.model.ClienteRef;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClienteRefRepository extends JpaRepository<ClienteRef, String> {
}
